<view class="binding">
  <view class="title" wx:if="{{isBinding}}">绑定PC账号</view>
  <view class="title" wx:if="{{!isBinding}}">注册PC账号</view>
  <view class="input-wrapper">
    <input bindinput="watchAccount" placeholder="账号" placeholder-style="color:#F6705A" value="{{account}}"></input>
    <input bindinput="watchPassword" placeholder="密码" placeholder-style="color:#F6705A" value="{{passowrd}}"></input>
  </view>
  <view wx:if="{{isBinding}}"><t-button text="绑定" type="change" size="mini" bindtap='submit'></t-button></view>
  <view wx:if="{{!isBinding}}"><t-button text="注册" type="change" size="mini" bindtap='submit'></t-button></view>
  <view class="tips" bindtap="goToSignUp" wx:if="{{isBinding}}">没有PC账号，快去注册吧兄弟（点击注册）</view>
  <view class="tips" bindtap="goToBinding" wx:if="{{!isBinding}}">绑定PC账号，立即同步数据（点击绑定）</view>
  <image src="../../images/tomato-bg.png" class="bg"></image>
</view>